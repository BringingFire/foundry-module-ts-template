<section>
  <header class="flex-header">
    <h1 class="grow-header">
      Dogs
    </h1>
    <button
      class="module-control"
      type="button"
      title="Fetch random dog picture"
      data-action="randomize-dog"
    >
      <i class="fas fa-random fa-fw"></i>
    </button>
  </header>
</section>
<section class="module-dog-picture-container">
  {{#if this.imageUrl}}
    <img class="module-dog-image" src="{{this.imageUrl}}" alt="Image of a random dog" />
  {{else}}
    <p class="module-dog-placeholder">
      🐶
    </p>
  {{/if}}
</section>